# 🌡️ Weather Parser - Анализатор данных о температуре

Модульное приложение для парсинга, хранения и анализа данных о температуре с полным набором юнит-тестов.

## 📋 Описание

Приложение позволяет:
- 📊 Просматривать архив измерений температуры
- ➕ Добавлять новые измерения в реальном времени
- 💾 Сохранять данные в файлы
- 📂 Загружать данные из файлов
- 📈 Вычислять статистику (минимум, максимум, среднее)

## 🏗️ Архитектура

```
├── models.py                # Модели данных (dataclass TemperatureMeasurement)
├── parsers.py              # Парсинг данных (дата, время, числа)
├── file_operations.py       # Работа с файлами и токенизация
├── ui.py                    # Интерактивное меню (Command Pattern со словарём)
├── main.py                  # Точка входа приложения
├── test_parsers.py          # 37 тестов для парсеров
├── test_file_operations.py  # 24 теста для файловых операций
└── temperature_input.txt    # Пример входных данных
```

## 🚀 Запуск приложения

### Основной запуск

```bash
python main.py temperature_input.txt
```

### Интерактивное меню

После запуска выберите нужное действие:

```
📊 ТЕМПЕРАТУРНЫЙ МОНИТОР
1. 📋 Просмотреть данные
2. ➕ Добавить новое измерение
3. 💾 Сохранить данные в файл
4. 📂 Загрузить данные из файла
5. ❌ Выход
```

## 🧪 Запуск тестов

### Запустить все тесты (61 тест)

```bash
python -m unittest discover -s . -p "test_*.py" -v
```

### Запустить только тесты парсеров (37 тестов)

```bash
python -m unittest test_parsers -v
```

### Запустить только тесты файловых операций (24 теста)

```bash
python -m unittest test_file_operations -v
```

### Запустить конкретный класс тестов

```bash
python -m unittest test_parsers.TestDateParser -v
python -m unittest test_parsers.TestFloatParser -v
python -m unittest test_file_operations.TestFileOperations -v
```

### Запустить конкретный тест

```bash
python -m unittest test_parsers.TestDateParser.test_valid_date -v
python -m unittest test_file_operations.TestFileOperations.test_roundtrip_save_and_load -v
```

## 📊 Статистика тестов

| Модуль | Классы | Тесты | Статус |
|--------|--------|-------|--------|
| test_parsers.py | 6 классов | 37 тестов | ✅ All passed |
| test_file_operations.py | 5 классов | 24 теста | ✅ All passed |
| **Всего** | **11 классов** | **61 тест** | **✅ Все прошли** |

### Покрытие тестами

**Парсеры:**
- ✅ DateParser - парсинг дат (6 тестов)
- ✅ TimeParser - парсинг времени (6 тестов)
- ✅ IntParser - парсинг целых чисел (6 тестов)
- ✅ FloatParser - парсинг чисел с плавающей точкой (8 тестов)
- ✅ StrParser - парсинг строк (3 теста)
- ✅ TryParse - попытка парсинга (8 тестов)

**Файловые операции:**
- ✅ Tokenize - разбиение на токены (6 тестов)
- ✅ BuildObject - построение объектов (10 тестов)
- ✅ FileOperations - работа с файлами (5 тестов)
- ✅ TemperatureMeasurement - модель данных (3 теста)

## 📝 Формат входных данных

```
temperature 2025.12.31 "Amsterdam Science Park" 21.5
temperature 2025.12.30 "Rotterdam Center" 7,2
temperature 2025.11.15 "Utrecht Station" 3.8
```

**Формат:** `temperature <дата> "<место>" <температура>`
- Дата: `YYYY.MM.DD` (обязательно)
- Место: строка в кавычках
- Температура: число (поддерживает точку и запятую как разделитель)

## 🎯 Особенности

### Интеллектуальный парсинг

Приложение автоматически определяет типы данных и может восстановиться от ошибок формата:
- ✅ Поддержка перепутанного порядка полей
- ✅ Поддержка разных форматов чисел (точка, запятая)
- ✅ Детальная обработка ошибок

### Command Pattern

Меню реализовано через словарь команд для легкого расширения функционала:

```python
MENU_COMMANDS: Dict[str, Callable] = {
    "1": lambda objs: view_data(objs),
    "2": lambda objs: add_measurement(objs),
    "3": lambda objs: save_data(objs),
    ...
}
```

### Модульная архитектура

Каждый модуль отвечает за свою область:
- `models.py` - только модели данных
- `parsers.py` - только парсинг
- `file_operations.py` - только файловые операции
- `ui.py` - только пользовательский интерфейс

## 📦 Зависимости

Только встроенные библиотеки Python 3.7+:
- `dataclasses`
- `datetime`
- `re`
- `typing`
- `unittest`

## 🔄 История разработки

1. ✅ Температурный парсер с умным определением типов
2. ✅ Интерактивное меню для управления данными
3. ✅ Красивый вывод с эмодзи и форматированием
4. ✅ Разделение на модули
5. ✅ 61 юнит-тест (100% успешно)
6. ✅ Документация с примерами

## 👨‍💻 Автор

koladakich

## 📄 Лицензия

MIT
